name: core
description: A new Flutter package project.
version: 0.0.1
publish_to: "none"

environment:
  sdk: '>=3.0.1 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  commons:
    path: "../commons"
  dependencies:
    path: "../dependencies"
  design_system:
    path: "../design_system"
  micro_app_splash:
    path: "../micro_app_splash"
  micro_app_login:
    path: "../micro_app_login"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter: